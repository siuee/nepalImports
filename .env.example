# Upstash Redis (https://vercel.com/marketplace/upstash) — price snapshot cache
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Vercel Cron: GET /api/cron/refresh-prices — Vercel sends Authorization: Bearer <this value>
CRON_SECRET=
